import React, { useState } from "react";
const Header = () => {
  return (
    <>
      <nav className="w-full h-auto bg-slate-500 flex">
        
      </nav>
    </>
  );
};

export default Header;
